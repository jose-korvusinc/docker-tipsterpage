resolver 127.0.0.11 valid=30s;
# Configuración Global de Gateway

server {
    listen 80;
    listen 443 ssl;
    server_name tipsterpage.test;
    ssl_certificate /etc/nginx/certs/apache.crt;
    ssl_certificate_key /etc/nginx/certs/apache.key;
    location / {
        proxy_pass http://landing:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

server {
    listen 80;
    listen 443 ssl;
    server_name app.affiliabet.test;
    ssl_certificate /etc/nginx/certs/apache.crt;
    ssl_certificate_key /etc/nginx/certs/apache.key;
    location / {
        proxy_pass http://affiliabet:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

server {
    listen 80;
    listen 443 ssl;
    server_name app.bagon.test;
    ssl_certificate /etc/nginx/certs/apache.crt;
    ssl_certificate_key /etc/nginx/certs/apache.key;
    location / {
        proxy_pass http://apibagon:4002;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

server {
    listen 80;
    listen 443 ssl;
    server_name ekans.test;
    ssl_certificate /etc/nginx/certs/apache.crt;
    ssl_certificate_key /etc/nginx/certs/apache.key;
    location / {
        proxy_pass http://apiekans:4003;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

server {
    listen 80;
    listen 443 ssl;
    server_name pikachu.test;
    ssl_certificate /etc/nginx/certs/apache.crt;
    ssl_certificate_key /etc/nginx/certs/apache.key;
    location / {
        proxy_pass http://pikachu-api:4001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

server {
    listen 80;
    listen 443 ssl;
    server_name admin.test;
    ssl_certificate /etc/nginx/certs/apache.crt;
    ssl_certificate_key /etc/nginx/certs/apache.key;
    location / {
        proxy_pass http://admin:4200;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

server {
    listen 80;
    listen 443 ssl;
    server_name app.backend.test;
    ssl_certificate /etc/nginx/certs/apache.crt;
    ssl_certificate_key /etc/nginx/certs/apache.key;
    location / {
        proxy_pass http://backend:2030;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
